import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from 'react';
import type {LogEntry,PipelineResponse,Status} from '../types';
type Theme='light'|'dark';
interface AppState {theme:Theme;toggleTheme:()=>void;developer:boolean;setDeveloper:(v:boolean)=>void;result:PipelineResponse|null;setResult:(v:PipelineResponse|null)=>void;runStatus:Status;setRunStatus:(v:Status)=>void;logs:LogEntry[];addLog:(level:LogEntry['level'],message:string)=>void;logsOpen:boolean;setLogsOpen:(v:boolean)=>void}
const Context=createContext<AppState|null>(null);
function initialTheme():Theme{const saved=localStorage.getItem('fello-theme');if(saved==='dark'||saved==='light')return saved;return matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'}
export function AppProvider({children}:{children:ReactNode}){const [theme,setTheme]=useState<Theme>(initialTheme);const [developer,setDeveloper]=useState(false);const [result,setResult]=useState<PipelineResponse|null>(null);const [runStatus,setRunStatus]=useState<Status>('idle');const [logsOpen,setLogsOpen]=useState(false);const [logs,setLogs]=useState<LogEntry[]>([]);useEffect(()=>{document.documentElement.dataset.theme=theme;localStorage.setItem('fello-theme',theme)},[theme]);const value=useMemo(()=>({theme,toggleTheme:()=>setTheme(t=>t==='light'?'dark':'light'),developer,setDeveloper,result,setResult,runStatus,setRunStatus,logs,addLog:(level:LogEntry['level'],message:string)=>setLogs(l=>[...l,{id:crypto.randomUUID(),time:new Date().toLocaleTimeString(),level,message}]),logsOpen,setLogsOpen}),[theme,developer,result,runStatus,logs,logsOpen]);return <Context.Provider value={value}>{children}</Context.Provider>}
export const useApp=()=>{const value=useContext(Context);if(!value)throw new Error('useApp requires AppProvider');return value};
