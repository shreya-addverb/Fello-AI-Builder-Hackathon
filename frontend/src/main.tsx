import {StrictMode,Suspense,lazy} from 'react';import {createRoot} from 'react-dom/client';import {BrowserRouter,Route,Routes} from 'react-router-dom';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import {AppProvider} from './context/app-context';import {Layout} from './components/layout';import './styles.css';
const Dashboard=lazy(()=>import('./pages/dashboard'));const Analysis=lazy(()=>import('./pages/analysis'));const PipelinePage=lazy(()=>import('./pages/secondary').then(m=>({default:m.PipelinePage})));const LogsPage=lazy(()=>import('./pages/secondary').then(m=>({default:m.LogsPage})));const SystemPage=lazy(()=>import('./pages/secondary').then(m=>({default:m.SystemPage})));const AboutPage=lazy(()=>import('./pages/secondary').then(m=>({default:m.AboutPage})));
const queryClient=new QueryClient({defaultOptions:{queries:{staleTime:10000}}});
createRoot(document.getElementById('root')!).render(<StrictMode><QueryClientProvider client={queryClient}><AppProvider><BrowserRouter><Suspense fallback={<div className="app-loader"><span/></div>}><Routes><Route element={<Layout/>}><Route index element={<Dashboard/>}/><Route path="analyze/company" element={<Analysis/>}/><Route path="analyze/visitor" element={<Analysis/>}/><Route path="pipeline" element={<PipelinePage/>}/><Route path="logs" element={<LogsPage/>}/><Route path="system" element={<SystemPage/>}/><Route path="about" element={<AboutPage/>}/></Route></Routes></Suspense></BrowserRouter></AppProvider></QueryClientProvider></StrictMode>);
